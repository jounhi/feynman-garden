---
title: 移动端响应式排版重构
date: 2026-05-10
categories: [fix, design]
---

# 2026-05-10 — 移动端响应式排版重构

## 修改

- **`blog/index.astro`**：移除卡片移动端居中对齐，统一左对齐风格；增大卡片间距（0.75em→1rem）；标题行高从1→1.2；hover时取消border-left避免布局抖动；分类标题允许flex-wrap；适配标题/日期/内边距字号
- **`changelog/index.astro`**：新增完整移动端媒体查询（此前完全缺失响应式样式）；标签添加 `.tags` flex容器管理换行；适配条目内边距、标题层级（h1/h2/h3）、标签字号
- **`blog/index.astro`**（第二版）：重新设计移动端卡片为列表式布局——透明背景+底部分隔线、水平排列（缩略图\|标题+日期\|箭头）、分类标题增加左侧accent强调线
- **`changelog/index.astro`**（第二版）：修复 CSS 特异性 bug——scoped `max-width:800px` 覆盖了 global `max-width:calc(100%-2em)`，导致移动端 main 溢出 345px；改用 `width:min()` 内建响应式；列表项和段落增加移动端字号适配
- **`.github/workflows/deploy.yml`**：添加 `set -e`（防止静默失败）、`git fetch origin main` 指定分支、echo 诊断日志
